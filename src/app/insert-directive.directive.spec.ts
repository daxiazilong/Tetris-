import { InsertDirectiveDirective } from './insert-directive.directive';

describe('InsertDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
